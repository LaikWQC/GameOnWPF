<UserControl x:Class="MyGameTest.Views.GameMainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyGameTest.Views"
        xmlns:models="clr-namespace:MyGameTest.Models"    
        mc:Ignorable="d">
    <TabControl>
        <TabItem Header="Battle" DataContext="{Binding BattleSectionVM}">
            <Grid DataContext="{Binding CurrentLocation}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <local:UnitView Grid.Column="0" DataContext="{Binding Heroes}"/>
                <local:UnitView Grid.Column="1" DataContext="{Binding Enemies}"/>
            </Grid>
        </TabItem>
    </TabControl>
</UserControl>
